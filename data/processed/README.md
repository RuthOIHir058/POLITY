# Processed data directory

Generated intermediate datasets belong here and are ignored by Git. The release includes the verified SQLite snapshot at `data/database/polity.db`.

Do not commit local exports, temporary joins, notebook caches, or provider workbooks. A database update must be reproducible from documented source identifiers and pass every validation command in `VERIFICATION_REPORT.md`.
